<Window x:Class="LabControl.Client.Kiosk.AlertaMensajeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aviso de Laboratorio" Height="280" Width="480"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterScreen">
    <Border Background="#0F172A" CornerRadius="16" BorderBrush="#F59E0B" BorderThickness="2" Padding="24">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="#F59E0B" Opacity="0.3" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Encabezado con Icono -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <Border Background="#37260C" CornerRadius="8" Width="42" Height="42" Margin="0,0,14,0">
                    <TextBlock Text="📢" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AVISO DEL ADMINISTRADOR" FontSize="15" FontWeight="Bold" Foreground="#F59E0B"/>
                    <TextBlock x:Name="TxtTimestamp" Text="Centro de Cómputo" FontSize="11" Foreground="#94A3B8"/>
                </StackPanel>
            </StackPanel>

            <!-- Cuerpo del Mensaje -->
            <Border Grid.Row="1" Background="#1E293B" CornerRadius="10" Padding="14" Margin="0,0,0,18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtMensaje" Text="Mensaje de alerta del laboratorio..." 
                               FontSize="14" Foreground="#F8FAFC" TextWrapping="Wrap" LineHeight="20"/>
                </ScrollViewer>
            </Border>

            <!-- Botón de Confirmación -->
            <Button Grid.Row="2" Content="Entendido / Aceptar" Height="42" 
                    Background="#F59E0B" Foreground="#0F172A" FontSize="14" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="OnAceptarClick">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Border>
</Window>
