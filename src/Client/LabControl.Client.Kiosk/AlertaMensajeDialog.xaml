<Window x:Class="LabControl.Client.Kiosk.AlertaMensajeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aviso de Laboratorio" Height="280" Width="480"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterScreen">
    <Border Background="{StaticResource UnivalleCardBgBrush}" CornerRadius="16" BorderBrush="{StaticResource UnivalleGuindoBrush}" BorderThickness="1.5" Padding="24">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#9E0044" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Encabezado con Icono -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <Border Background="{StaticResource UnivallePillBgBrush}" BorderBrush="{StaticResource UnivallePillBorderBrush}" BorderThickness="1" CornerRadius="10" Width="44" Height="44" Margin="0,0,14,0">
                    <TextBlock Text="📢" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AVISO DEL ADMINISTRADOR" FontSize="14" FontWeight="Bold" Foreground="{StaticResource UnivallePillTextBrush}"/>
                    <TextBlock x:Name="TxtTimestamp" Text="Centro de Cómputo" FontSize="11" Foreground="{StaticResource UnivalleTextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <!-- Cuerpo del Mensaje -->
            <Border Grid.Row="1" Background="#0D0F14" BorderBrush="{StaticResource UnivalleCardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtMensaje" Text="Mensaje de alerta del laboratorio..." 
                               FontSize="14" Foreground="#F8FAFC" TextWrapping="Wrap" LineHeight="20"/>
                </ScrollViewer>
            </Border>

            <!-- Botón de Confirmación -->
            <Button Grid.Row="2" Content="Entendido / Aceptar" Height="42" 
                    Style="{StaticResource UnivallePrimaryButton}" Click="OnAceptarClick"/>
        </Grid>
    </Border>
</Window>
